move the zipped file to a directory without spaces. 

Build it using 
r CMD BUILD 

Then Install it using the GUI (if built into a binray .zip file) or using the command prompt for .tar files:

r CMD INSTALL
